namespace QCManagement.IntegrationTests.Repositories.AuditLog
{
    /// <summary>
    /// Placeholder — QCManagement's AuditLogMongoRepository targets MongoDB, not SQL Server.
    /// Integration tests for MongoDB repositories require a live MongoDB instance and are
    /// not covered by the SQL-based DbFixture. Add real tests here when a MongoDB test
    /// infrastructure (e.g. Mongo2Go or a containerised instance) is available.
    /// Replace with real EF Core / Dapper repository tests once SQL entities are added.
    /// </summary>
    public class AuditLogMongoRepositoryPlaceholderTests
    {
        [Fact]
        public void Placeholder_MongoRepositoryTestsNotYetImplemented_Passes()
        {
            true.Should().BeTrue();
        }
    }
}
