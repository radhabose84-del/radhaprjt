namespace QCManagement.UnitTests.Domain
{
    /// <summary>
    /// Placeholder — QCManagement has no EF Core BaseEntity entities in the initial scaffold.
    /// Add real entity tests here as business entities (e.g. QCParameter, InspectionOrder) are implemented.
    /// </summary>
    public class BaseEntityAuditFieldsTests
    {
        [Fact]
        public void Placeholder_NoBaseEntityEntitiesYet_Passes()
        {
            // QCManagement scaffold contains only AuditLogs (MongoDB) and JwtSettings (config).
            // Neither extends BaseEntity. This test exists so the Domain/ folder is not empty.
            // Replace with real audit-field tests once the first EF Core entity is added.
            true.Should().BeTrue();
        }
    }
}
